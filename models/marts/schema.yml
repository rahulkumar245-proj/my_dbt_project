version: 2

models:
  - name: dim_customers
    description: "Customer dimension table."

    columns:
      - name: CustomerKey
        data_tests:
          - not_null
          - unique
          
      - name: CustomerID
        data_tests:
          - not_null
          - unique

      - name: CustomerName
        data_tests:
          - not_null

  - name: dim_products
    description: "Product dimension table."

    columns:
      - name: ProductID
        data_tests:
          - not_null
          - unique

      - name: ProductName
        data_tests:
          - not_null

  - name: fct_orders
    description: "Order-level fact table containing sales transactions and order attributes."

    columns:
      - name: OrderID
        description: "Unique identifier for each order."
        data_tests:
          - not_null
          - unique

      - name: CustomerID
        description: "Identifier of the customer who placed the order."
        data_tests:
          - not_null
          - relationships:
              arguments:
                to: ref('dim_customers')
                field: CustomerID

      - name: ProductID
        data_tests:
          - not_null
          - relationships:
              arguments:
                to: ref('dim_products')
                field: ProductID

      - name: TotalAmount
        description: "Final monetary amount of the order."
        data_tests:
          - not_null
      
      - name: Quantity
        data_tests:
          - positive_value
      
      - name: UnitPrice
        data_tests:
          - positive_value

      - name: OrderStatus
        data_tests:
          - accepted_values:
              arguments:
                values: ['Cancelled', 'Delivered', 'Pending', 'Returned', 'Shipped']
              config:
                severity: error